{
    "name": "nf/calculator",
    "version": "1.0.0",
    "autoload": {
        "psr-4": {
            "": "src/"
        }
    },
    "scripts": {
        "test": "phpunit --bootstrap vendor/autoload.php --colors=always tests"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.1"
    }
}
